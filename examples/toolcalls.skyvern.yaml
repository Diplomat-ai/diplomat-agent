# toolcalls.yaml — Skyvern scan excerpt (3 of 382 tool calls)
# Generated by agent-canary · https://github.com/Diplomat-agents/agent-canary

summary:
  total: 382
  no_checks: 307
  partial_checks: 66
  confirmed: 9

tool_calls:

  # ⚠ NO CHECKS — database_write
  - function: _create_screenshot_after_execution
    file: skyvern/core/script_generations/script_skyvern_page.py
    line: 517
    actions:
      - "await app.ARTIFACT_MANAGER.create_artifact("
    checks: []
    missing:
      - "auth check"
      - "rate limit"

  # ~ PARTIAL CHECKS — database_write
  - function: create_or_update_script_block
    file: skyvern/core/script_generations/generate_script.py
    line: 3208
    actions:
      - "await app.ARTIFACT_MANAGER.create_script_file_artifact("
    checks:
      - type: input_validation
        coverage: partial
    missing:
      - "auth check"
      - "idempotency key"

  # ✓ CONFIRMED — database_write (idempotency)
  - function: _get_or_create_browser_state
    file: skyvern/core/script_generations/script_skyvern_page.py
    line: 81
    actions:
      - "await app.BROWSER_MANAGER.get_or_create_for_workflow_run("
      - "await app.BROWSER_MANAGER.get_or_create_for_script("
    checks:
      - type: idempotency_key
        coverage: full
    missing: []
